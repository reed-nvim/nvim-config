return {
  "kylechui/nvim-surround",
  ersion = "*", -- Use for stability; omit to use `main` branch for the latest features
  vent = "VeryLazy",
  onfig = function()
    require("nvim-surround").setup {
      -- Configuration here, or leave empty to use defaults
    }
  end,
}
